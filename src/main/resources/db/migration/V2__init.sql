CREATE TABLE calculator.Event (
                       id VARCHAR(150) NOT NULL,
                       date DATE,
                       localization VARCHAR(255),
                       description VARCHAR(255),
                       PRIMARY KEY(id)
);
